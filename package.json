{
  "name": "cacophony",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adversarial pull request reviews from configurable agent lenses.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
